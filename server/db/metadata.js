// Stage 3+ adds per-project metadata helpers (statuses, categories,
// priorities) here. Each exported function takes the better-sqlite3 handle as
// its first argument so callers can pass a transaction-bound db.
